# breezy-meals.com

This repository is configured for GitHub Pages with the custom domain:

- `breezy-meals.com` (via `CNAME`)

It also includes a `.nojekyll` file so static assets are served without Jekyll processing, matching the deployment token/setup used in the related domain repositories.
